{
  "name": "SpectrumAnalyzer",
  "version": "0.2.0",
  "description": "audio spectrum analyzer",
  "main": "index.js",
  "dependencies": {
    "azure-storage": "^2.10.0"
  },
  "scripts": {
    "sync-code": "git add . && git commit -m \"wip\" && git push origin develop && az functionapp deployment source sync --name azurehacks --resource-group azurehacks"
  }
}
